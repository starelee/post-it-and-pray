# post-it-and-pray — BACKLOG

지금 당장 만들지 않는, 추후 고려할 아이디어 모음. `TODO.md`(현재 작업 스펙)와는 별개 문서.

---

- [ ] **닌자 모드 표시 형식 유저 설정**: 지금은 일관성을 위해 체크박스(todo-list) 형태로 고정하지만, 추후 설정에서 "자유 텍스트 모드"와 "체크리스트 모드" 중 유저가 선택할 수 있게 하는 기능. 저장 형식이 서로 다르므로(텍스트 blob vs task 배열) 구현 시 마이그레이션 전략 필요.

- [ ] **미친 개빡쳐 야근 모드**: 레이아웃과 문구가 통째로 "흑화"하는 테마 모드. 기존 다크 모드(`prefers-color-scheme`)가 색상 토큰만 바꾸는 것과 달리, 이건 카피 자체도 같이 바뀌어야 함 — 예: `gotta do...`, empty-hint(`오늘은 뭘 해볼까?`), 버튼 라벨 등 UI 텍스트 전반이 더 격해진 톤으로 교체. 구현 시 결정 필요한 것:
  - 트리거 방식: 수동 토글(라이트/다크 토글 옆에 추가) vs 특정 시각 이후(예: 밤 9시~) 자동 전환 vs 둘 다
  - 색상 톤: 다크 모드와는 별개의 팔레트(더 붉고 채도 높은 "분노" 톤)로 새로 설계 필요
  - 문구 교체 대상 범위: 고정 라벨(타이틀, 빈 상태 문구 등)만 바꿀지, 유저가 입력한 실제 할 일 텍스트에도 어떤 식으로든 영향(예: 랜덤 접미사)을 줄지

- [ ] **나 방금 출근했어 모드 (GTD 리프레시)**: 어제자에 못 끝낸 일을 오늘자로 정리해주는 기능. 구현 시 결정 필요한 것:
  - 대상 범위: `today`의 plain 항목은 원래 날짜 개념 없이 체크 전까지 계속 남아있는 구조라 별도 처리가 필요 없음 — 이 기능이 실질적으로 다루게 될 건 `scheduled` 중 마감일이 지난 "oops, still here" 항목들일 가능성이 큼. 이걸 오늘 날짜로 `dueDate` 갱신할지, 아니면 날짜를 아예 떼고 plain `today`로 편입시킬지 결정 필요
  - 트리거 방식: 자동(앱 열 때마다 조용히 처리, 5항의 자정 스윕과 유사) vs 수동("출근했어" 버튼을 직접 눌러야 실행) — 수동 쪽이 이름과 더 잘 맞고, GTD의 "위클리 리뷰"처럼 유저가 의식적으로 트리거하는 의례적 행동에 가까움
  - 실행 시 UI: 조용히 뒤에서 정리만 할지, 아니면 "어제 이거 못 끝냈는데 오늘로 가져올까?" 하고 항목별로 확인받는 리뷰 화면을 보여줄지

- [ ] **폰트 변경**: 웹폰트 몇가지 옵션 선택해서 변경해주는 기능

- [ ] **소스 분리**: 즉 내가 이 프로젝트라면 굳이 복잡한 merge 시스템 안 만들고 비로그인=localStorage / 로그인=Supabase로 소스를 완전히 분리할 것 같아. 지금 규모에는 그게 더 맞아 보여.
- [ ] **보안 강화** : 현재 저장소 최상위에는 SQL migration이나 RLS policy 정의가 없어서 GitHub 코드만 보고 실제로 그 제약이 설정되어 있는지는 검증할 수 없다. 공개 프로젝트로 키울 생각이면 supabase/schema.sql 같은 파일로 테이블과 RLS 정책을 버전 관리하는 걸 강하게 추천해.
- [ ] **이름 변경** : post it and pray > post it 못씀. and pray도 너무 종교적.. 일단 후보는 desk mate (somehow working)임 근데 너무 기네
